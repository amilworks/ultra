# Direct dependencies for the isolated Kawin kinetics image. Kawin 0.5.0
# requires Python >=3.11 and NumPy >=2; this lock must never be installed into
# the shared NumPy-1.26 code-execution image.
kawin==0.5.0
numpy==2.4.6
pycalphad==0.11.2
scipy==1.17.1
pytest==8.4.2
