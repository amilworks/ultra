# Schema-owner credentials used only by `ultra-control migrate` during a
# control/edge deploy. Install separately from ultra-backend.env so the serving
# process and compute workers never receive the migration credential.
ULTRA_CONTROL_MIGRATION_DATABASE_URL=postgresql://ultra_migration:change-me-migration-db@127.0.0.1:55433/ultra
